- if @unfilled_time_sheet_report.present?
  %span Employees who have not filled the timesheet in last week.
  %br
  %br
  %table{:border => "1"}
    %tr
      %th Name
      %th Project name
    - @unfilled_time_sheet_report.each do |reports|
      %tr
        -reports.each do |report|
          %td= report
%br
%span Weekly timesheet report from #{Date.today - 7.days} to #{Date.today - 1.days}
